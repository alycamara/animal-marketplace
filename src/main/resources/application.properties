# Configuration des vues
spring.mvc.view.prefix=/templates/
spring.mvc.view.suffix=.html

# Configuration de la base de données
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuration Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuration AWS S3
aws.s3.bucket-name=${S3_BUCKET_NAME}
aws.s3.region=${REGION}
aws.access-key-id=${ACCESS_KEY_ID}
aws.secret-access-key=${SECRET_ACCESS_KEY}

# Configuration du serveur
server.port=${PORT:8080}

# Configuration Thymeleaf
spring.thymeleaf.cache=false

# Configuration des logs
logging.level.root=INFO
logging.level.org.springframework.web=INFO
logging.level.com.camara.animalmarketplace=INFO

# Configuration des profils
spring.profiles.active=local

# Configuration Spring Security
spring.web.encoding.charset=UTF-8
spring.web.encoding.enabled=true
spring.web.encoding.force=true

# Configuration Amazon Cognito
spring.security.oauth2.client.registration.cognito.client-id=5b6fl7b124qgid3i07ji8m3m80
spring.security.oauth2.client.registration.cognito.client-secret=rseh41mq3dtagr4o5k5eb26eoabmg8tvbd4ustac746m85epcku
spring.security.oauth2.client.registration.cognito.scope=email,openid,phone
spring.security.oauth2.client.registration.cognito.redirect-uri=http://localhost:8080/login/oauth2/code/cognito
spring.security.oauth2.client.provider.cognito.issuerUri=https://cognito-idp.eu-west-3.amazonaws.com/eu-west-3_kgj9eipZs
spring.security.oauth2.client.provider.cognito.user-name-attribute=username
cognito.domain=https://eu-west-3kgj9eipzs.auth.eu-west-3.amazoncognito.com
cognito.logout-redirect-url=http://localhost:8080/ads

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
